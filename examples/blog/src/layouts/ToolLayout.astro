<style>
	.content-section {
		margin-top: 2.5rem;
		background: var(--bg-card);
		border: 1px solid var(--card-border);
		border-radius: 16px;
		padding: 2em;
		box-sizing: border-box;
		width: 100%;
	}
	.content-section h2 {
		font-size: 1.3rem;
		font-weight: 700;
		color: var(--text-main);
		margin-bottom: 1.2rem;
	}
	.related-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
		width: 100%;
		box-sizing: border-box;
	}
	.related-card {
		display: flex;
		align-items: center;
		gap: 0.8rem;
		padding: 0.9rem;
		background: var(--code-bg);
		border: 1px solid var(--card-border);
		border-radius: 10px;
		text-decoration: none !important;
		transition: border-color 0.2s, transform 0.2s;
		box-sizing: border-box;
		width: 100%;
		min-width: 0;
		height: auto; /* PC 端自适应高度 */
	}
	.related-card:hover {
		border-color: var(--accent);
		transform: translateY(-2px);
	}
	.related-card.sponsor-card {
		border-color: rgba(245, 158, 11, 0.4);
		background: linear-gradient(135deg, var(--code-bg) 0%, rgba(245, 158, 11, 0.05) 100%);
	}
	.card-icon {
		font-size: 1.6rem;
		flex-shrink: 0;
	}
	.card-body {
		flex: 1;
		min-width: 0;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}
	.card-header-row {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.5rem;
		margin-bottom: 0.25rem;
		min-width: 0;
	}
	.related-card h4 {
		margin: 0;
		color: var(--text-main);
		font-size: 0.92rem;
		font-weight: 700;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
		flex: 1;
		min-width: 0;
	}
	.related-card p {
		margin: 0;
		color: var(--text-muted);
		font-size: 0.8rem;
		line-height: 1.4;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
		word-break: break-word;
	}
	.sponsor-badge {
		font-size: 0.68rem;
		padding: 0.15em 0.5em;
		background: rgba(245, 158, 11, 0.15);
		color: #f59e0b;
		border: 1px solid rgba(245, 158, 11, 0.3);
		border-radius: 4px;
		font-weight: 700;
		text-transform: uppercase;
		flex-shrink: 0;
	}

	/* 📱 移动端自适应优化 */
	@media (max-width: 768px) {
		.content-section {
			padding: 1.25em 1rem;
		}
		.related-grid {
			grid-template-columns: 1fr; /* 单列全宽显示 */
			gap: 0.8rem;
		}
		.related-card {
			padding: 0.9rem 1rem;
			height: auto; /* 移动端完全自适应高度 */
			align-items: flex-start; /* 顶部对齐，适合多行文本排版 */
		}
		.card-icon {
			font-size: 1.5rem;
			margin-top: 0.1rem; /* 顶部微调对齐标题 */
		}
		.card-header-row {
			flex-wrap: wrap; /* 小屏上标题与赞助 Tag 允许换行 */
			align-items: flex-start;
			margin-bottom: 0.35rem;
		}
		.related-card h4 {
			white-space: normal; /* 取消单行限制，允许自然换行完整显示标题 */
			font-size: 0.95rem;
			line-height: 1.35;
		}
		.related-card p {
			-webkit-line-clamp: unset; /* 取消两行截断限制，随内容自然撑开 */
			font-size: 0.82rem;
			line-height: 1.45;
		}
	}
</style>
