---
// 1. 使用 src/ 路径别名，彻底修复 [UNRESOLVED_IMPORT] 找不到组件的构建报错
import BaseHead from 'src/components/BaseHead.astro';
import Header from 'src/components/Header.astro';
import Footer from 'src/components/Footer.astro';
import { SITE_TITLE } from 'src/consts';

const pageTitle = `提示词结构化优化工具 — ${SITE_TITLE}`;
const pageDescription = "使用 CO-STAR 框架，将粗糙的一句话需求一键转化为适用于 ChatGPT、Claude 和 DeepSeek 的专业结构化提示词。";
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={pageTitle} description={pageDescription} />
		<style>
			:root {
				--primary: #6366f1;
				--primary-hover: #4f46e5;
				--bg-card: #1e293b;
				--border-color: #334155;
				--text-main: #f8fafc;
				--text-muted: #94a3b8;
			}
			main {
				width: 800px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 3em 1em;
			}
			.tool-header {
				text-align: center;
				margin-bottom: 2em;
			}
			.tool-header h1 {
				font-size: 2.2rem;
				font-weight: 800;
				margin-bottom: 0.3em;
			}
			.tool-header p {
				color: var(--text-muted);
				font-size: 1.05rem;
			}
			.tool-box {
				background: var(--bg-card);
				border: 1px solid var(--border-color);
				border-radius: 12px;
				padding: 2em;
			}
			.form-group {
				margin-bottom: 1.5em;
			}
			label {
				display: block;
				font-weight: 600;
				margin-bottom: 0.5em;
				color: var(--text-main);
			}
			textarea, select {
				width: 100%;
				padding: 0.8em 1em;
				background: #0f172a;
				border: 1px solid var(--border-color);
				border-radius: 8px;
				color: #fff;
				font-size: 0.95rem;
				box-sizing: border-box;
				outline: none;
			}
			textarea:focus, select:focus {
				border-color: var(--primary);
			}
			.btn-generate {
				width: 100%;
				background: var(--primary);
				color: #fff;
				border: none;
				padding: 0.9em;
				font-size: 1rem;
				font-weight: 700;
				border-radius: 8px;
				cursor: pointer;
				transition: background 0.2s;
			}
			.btn-generate:hover {
				background: var(--primary-hover);
			}
			.result-area {
				margin-top: 2em;
				display: none;
			}
			.result-header {
				display: flex;
				justify-content: space-between;
				align-items: center;
				margin-bottom: 0.5em;
			}
			.btn-copy {
				background: rgba(255, 255, 255, 0.1);
				border: 1px solid var(--border-color);
				color: #fff;
				padding: 0.4em 0.8em;
				border-radius: 6px;
				cursor: pointer;
				font-size: 0.85rem;
			}
			.btn-copy:hover {
				background: rgba(255, 255, 255, 0.2);
			}
			pre {
				background: #0f172a;
				padding: 1.2em;
				border-radius: 8px;
				border: 1px solid var(--border-color);
				white-space: pre-wrap;
				word-break: break-word;
				color: #a5b4fc;
				font-size: 0.9rem;
				line-height: 1.6;
			}
		</style>
	</head>
	<body>
		<!-- 传入 lang="zh" 确保 Header 渲染中文导航 -->
		<Header lang="zh" />
		<main>
			<div class="tool-header">
				<h1>📝 提示词结构化优化工具</h1>
				<p>一键将简单想法转化为符合 CO-STAR 框架的高质量结构化 Prompt（适用于 ChatGPT / Claude / DeepSeek）</p>
			</div>

			<div class="tool-box">
				<div class="form-group">
					<label for="rawPrompt">输入你的原始想法 / 任务需求：</label>
					<textarea id="rawPrompt" rows="4" placeholder="例如：写一篇适合初学者看的 Shopify 独立站 SEO 优化技巧指南文章。"></textarea>
				</div>

				<div class="form-group">
					<label for="roleSelect">选择 AI 扮演的角色设定：</label>
					<select id="roleSelect">
						<option value="资深 SEO 专家与内容策略师">资深 SEO 专家与内容策略师</option>
						<option value="全栈软件开发专家">全栈软件开发专家</option>
						<option value="爆款文案与直接响应文案师">爆款文案与直接响应文案师</option>
						<option value="资深产品经理">资深产品经理</option>
						<option value="资深电商运营专家">资深电商运营专家</option>
					</select>
				</div>

				<button class="btn-generate" id="generateBtn">生成结构化提示词 ✨</button>

				<div class="result-area" id="resultArea">
					<div class="result-header">
						<label>优化后的 CO-STAR 结构化 Prompt：</label>
						<button class="btn-copy" id="copyBtn">📋 复制提示词</button>
					</div>
					<pre id="outputPrompt"></pre>
				</div>
			</div>
		</main>
		<!-- 传入 lang="zh" 确保 Footer 渲染中文版权 -->
		<Footer lang="zh" />

		<script>
			const generateBtn = document.getElementById('generateBtn');
			const copyBtn = document.getElementById('copyBtn');
			const rawPrompt = document.getElementById('rawPrompt') as HTMLTextAreaElement;
			const roleSelect = document.getElementById('roleSelect') as HTMLSelectElement;
			const resultArea = document.getElementById('resultArea') as HTMLDivElement;
			const outputPrompt = document.getElementById('outputPrompt') as HTMLPreElement;

			generateBtn?.addEventListener('click', () => {
				const input = rawPrompt.value.trim();
				if (!input) {
					alert('请先输入你的原始需求或想法！');
					return;
				}

				const role = roleSelect.value;

				// CO-STAR 中文结构化模板
				const structuredPrompt = `# 角色 (ROLE)
你是一位【${role}】，在该领域拥有丰富的实战经验和极高的专业造诣。

# 背景 (CONTEXT)
用户目前需要完成以下具体任务或目标："${input}"。

# 目标 (OBJECTIVE)
请根据行业最佳实践，提供一份全面、精准、极具可操作性的回答，完美解决上述需求。

# 风格与语气 (STYLE & TONE)
- 专业、条理清晰、简洁干练且富有吸引力。
- 拒绝任何空话、废话或无意义的客套话。
- 善用 Markdown 加粗重点、列表项或代码块，以极大化提升阅读体验。

# 目标受众 (AUDIENCE)
面向寻求即时、实用且无模糊地带的高效专业人士或从业者。

# 输出格式 (RESPONSE FORMAT)
请按照以下逻辑结构进行回答：
1. 核心总结 / 关键要点速览
2. 详细的分步实施计划与解决方案
3. 必须注意的避坑指南或潜在风险

如果你已完全理解上述要求，请直接开始回答。`;

				outputPrompt.textContent = structuredPrompt;
				resultArea.style.display = 'block';
			});

			copyBtn?.addEventListener('click', () => {
				navigator.clipboard.writeText(outputPrompt.textContent || '');
				copyBtn.textContent = '✅ 已复制！';
				setTimeout(() => {
					copyBtn.textContent = '📋 复制提示词';
				}, 2000);
			});
		</script>
	</body>
</html>
